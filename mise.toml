# Pinned toolchain. After cloning:
#   mise install
#   lefthook install

[tools]
go = "1.24"
# v2.x supports the v2-format .golangci.yml and is what CI installs
# via golangci-lint-action@v9. Local lint matches CI lint.
golangci-lint = "2"
lefthook = "1"
